# Copia a .env y rellena. .env no se versiona.
DB_PASSWORD=
# Client ID de tipo "Desktop app" de Google Cloud Console.
# La API valida contra el que se ponga aqui; vacio = auth desactivada (solo dev).
GOOGLE_CLIENT_ID=
