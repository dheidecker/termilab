<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Open SSH sessions keep running with the app in the background (SessionService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The updater hands the downloaded APK to the system installer (installApk). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <service
            android:name=".SessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the user's open SSH terminal sessions connected while the app is in the background. Runs only while at least one session is open and stops when the last one closes." />
        </service>
    </application>
</manifest>
